#include <bits/stdc++.h>

using namespace std;

int f(int i,int count, int curr_index){

}
int solve(int arr[],int n,int k){
    int i =0;
    int count = k;
    while(i<n){
        int curr_count += arr[i];
        continue = f(i, curr_count, i+1);
        skip = f(i, )  

    }
}

int main(){
    int n=8;

    int k=4;

    int arr[] = {-1,7,5,-3,-2,8,4,-1};

    solve(arr, n,k);
}